# Cycle 166: PLAN Stage

## Epic Selection
- **Selected Epic**: epic-050「质量基础设施升级与 E2E 稳定性修复」
- **Proposed by**: QA
- **Supported by**: PM-Mon, PM-Lean, ARCH
- **Priority**: high
- **Estimated Effort**: small
- **Rationale**: 1833 单元测试历史最高水位，但 E2E 测试仍有 skipped + flaky（pre-existing 空数据测试）。MoreMenu 导航定位依赖文本匹配不够稳定，需要 data-testid 稳定化。

## Iteration Breakdown
1. **iter-001**: E2E 测试稳定化修复 (in_progress)
2. **iter-002**: Playwright 并行执行配置 (pending)
3. **iter-003**: 视觉回归测试引入 (pending)
4. **iter-004**: 测试覆盖率目标 gate (pending)
5. **iter-005**: 性能回归自动检测 (pending)

## Iter-001 Implementation Plan
**Focus**: 给导航组件添加 data-testid，稳定 E2E 定位器

### Root Cause Analysis
E2E 测试 flaky 原因：
1. MoreMenu 导航依赖文本匹配 `filter({ hasText: /更多|More/i })` 不够稳定
2. MobileNav 缺少 data-testid，定位依赖文本
3. practice.spec.ts 没有使用 baseURL，依赖硬编码 localhost:3000

### 10-Step Plan
1. MoreMenu.tsx 添加 data-testid="more-menu-trigger" 等
2. MobileNav.tsx 添加 data-testid="mobile-nav" 等
3. complete-flow.spec.ts 改用 data-testid 选择器
4. data-management.spec.ts 改用 data-testid 选择器
5. practice.spec.ts 添加 baseURL 处理
6. 所有测试添加 waitForLoadState('networkidle')
7. 更新单元测试 mock
8. 运行 E2E 验证通过
9. 运行单元测试验证零回归
10. Build + Lint 验证

## Strategic Directives
- No active custom directives (dir-1778414197688 and dir-1778465917386 already executed in previous cycles)

## Next Step
- Move to IMPLEMENT stage
- Execute the 10-step plan
- Focus on making MoreMenu and MobileNav testable via stable selectors

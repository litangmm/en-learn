# Evolution Report — Cycle 38 (2026-05-10)

## 本次 Epic / Iteration 实现摘要

**Epic**: epic-006「前端架构债务清理与性能基线」
**Iteration**: iter-001「视图路由抽象 + P0/P1 Bug 修复」

### 核心成果

1. **ViewRouter 视图路由抽象**
   - 创建 `src/components/routing/ViewRouter.tsx` — view → component 映射表
   - 创建 `src/components/routing/NavigationContext.tsx` — 导航状态 Provider
   - 创建 `src/components/routing/index.ts` — barrel exports

2. **App.tsx 重构**
   - 替换原有的嵌套三元运算符视图渲染（~135 行条件渲染代码）
   - 使用 `<ViewRouter>` 和 `<NavigationProvider>` 包装
   - 移除了本地 View 类型定义，统一使用 NavigationContext

3. **Bug 修复**
   - **P0**: 切换词典时 `storage.clearSession()` 在 `confirmSwitch` 中已存在
   - **P1**: Input 组件已使用 `h-11 md:h-10`（移动端 44px，桌面端 40px）

4. **测试覆盖**
   - 新增 `ViewRouter.test.tsx` — 16 个测试覆盖全部 8 个视图映射和 props 传递
   - 修复 `LeaderboardEntry` 类型问题（在 App.leaderboard.test.tsx 中）

### 测试结果

| 类型 | 结果 |
|------|------|
| Lint | ✅ passed (4 pre-existing warnings in App.tsx, PracticeCard.tsx, NavigationContext.tsx) |
| Build | ✅ passed (built in 2.86s) |
| Unit Tests | ✅ **501/501 passed** (51 test files) |
| E2E | ⏭️ skipped (not last iteration — iter-002 and iter-003 pending) |

### Commit 信息

- `1e73d65` — `[EVOLUTION] epic-006 iter-001: ViewRouter + NavigationContext 重构，修复 P0/P1`
- `15ab7fe` — `[EVOLUTION] epic-006 iter-001: 进化文档（ViewRouter + NavigationContext 重构）`

---

## 修改文件列表

### 新增文件
- `src/components/routing/ViewRouter.tsx`
- `src/components/routing/NavigationContext.tsx`
- `src/components/routing/index.ts`
- `src/components/routing/__tests__/ViewRouter.test.tsx`

### 修改文件
- `src/App.tsx` — 移除 ~135 行嵌套三元运算符，使用 ViewRouter + NavigationProvider
- `src/components/PracticeCard.tsx` — 确认 h-11 md:h-10 输入框高度
- `src/components/__tests__/App.leaderboard.test.tsx` — 修复 LeaderboardEntry 类型

---

## Epic 状态

**epic-006「前端架构债务清理与性能基线」** — 进行中

| Iteration | Title | Status |
|-----------|-------|--------|
| iter-001 | 视图路由抽象 + P0/P1 Bug 修复 | ✅ completed |
| iter-002 | PracticeCard 策略模式重构 + 排行榜 Loading | ⏳ pending |
| iter-003 | 构建体积监控基线 | ⏳ pending |

---

## 头脑风暴候选（未选中的 Epic）

| Epic | 提案方 | 支持方 | 优先级 | 状态 |
|------|--------|--------|--------|------|
| epic-005 | PM-Growth | 4/9 | medium | 储备 |
| epic-007 | PM-Eco | 3/9 | low | 储备 |
| epic-008 | EXP | 2/9 | low | 储备 |

---

## 经验沉淀

### ARCH 表现分析

**本次成功入选的原因**：
- App.tsx 复杂度已达灾难级临界点（~300 行条件渲染代码）
- 技术债务指数级累积，重构窗口正在加速关闭
- 8/9 Agent 一致支持（仅自己未自投）

**成功模式**：
- 持续追踪 App.tsx 条件渲染行数，从 cycle-14 开始记录
- 将架构债务与具体功能进度关联（epic-003 每完成一个迭代增加复杂度）
- 准确的时机判断：在 epic-003 完成后立即推动 epic-006

### implement 流程表现

**有效模式**：
- 视图路由抽象的 view → component 映射设计简洁可扩展
- NavigationContext 的 Provider 模式实现了状态共享与视图解耦
- P0/P1 Bug 修复在重构期间一并处理，体现了重构的价值

**观察**：
- 重构期间未引入新依赖，零构建体积增长
- 测试覆盖（16 个 ViewRouter 测试）确保了重构安全性

### test 流程表现

**有效模式**：
- ViewRouter.test.tsx 的 16 个测试覆盖了全部 8 个视图映射和 props 传递
- 修复 LeaderboardEntry 类型问题展示了快速响应能力
- 501 测试全部通过，项目历史测试高水位

---

## 下一轮预测

### 预期进度

**下一 iteration**: epic-006 iter-002「PracticeCard 策略模式重构 + 排行榜 Loading」

**关键任务**：
1. PracticeCard 视图条件渲染重构为策略模式
2. 排行榜加载状态（P2 Deferred Directive）
3. 预期测试通过率：510+/510+

**注意**：epic-006 还有 2 个 pending iterations，run.sh 会自动检测并继续 PLAN 阶段。
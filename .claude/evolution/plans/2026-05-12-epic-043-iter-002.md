# Implementation Plan: epic-043 iter-002

## Iteration Overview

- **Epic**: epic-043 词典数据架构升级与查询性能优化
- **Iteration**: iter-002 按需加载与懒加载策略
- **Scope**: frontend-only
- **Previous Iteration**: iter-001 (词典索引结构重构) - completed

## Goals

1. 实现多词典按需加载管理
2. 扩展 useDictionaryIndex 支持多词典
3. 集成 dictionaryCache 到 hook
4. 确保索引在切换词典时正确重建

## Implementation Steps

### Step 1: 检查 dictionaryCache 集成

检查 `dictionaryCache.ts` 和 `loader.ts` 与 `useDictionaryIndex.ts` 的集成情况。

- **文件**: `src/hooks/useDictionaryIndex.ts`, `src/data/dictionaryCache.ts`, `src/data/loader.ts`
- **验证**: useDictionaryIndex 是否已正确使用 getCachedDictionary 和 clearDictionaryCache

### Step 2: 多词典状态管理

扩展 useDictionaryIndex 支持管理多个词典的索引状态。

- **文件**: `src/hooks/useDictionaryIndex.ts`
- **修改**: 增强状态管理，支持 currentId 切换后索引正确加载

### Step 3: 多词典切换测试

验证词典切换场景下索引行为正确。

- **文件**: `src/hooks/__tests__/useDictionaryIndex.test.ts`
- **测试场景**:
  - 加载词典 A → 构建索引
  - 切换到词典 B → 构建新索引
  - 切换回词典 A → 索引已存在，跳过重建
  - 清除缓存后重新加载 → 索引重建

### Step 4: 集成 DictionaryBrowser

在 DictionaryBrowser 中使用 useDictionaryIndex。

- **文件**: `src/components/DictionaryBrowser.tsx`
- **修改**: 使用 useDictionaryIndex 替代直接 loadDictionary 调用

### Step 5: 全量测试回归

运行完整测试套件确保零回归。

- **命令**: `npm test`
- **预期**: 所有测试通过

## Files to Modify

1. `src/hooks/useDictionaryIndex.ts` - 多词典状态管理增强

## Files to Create

1. `src/hooks/__tests__/useDictionaryIndex.multi-dict.test.ts` - 多词典切换测试（可选，扩展现有测试）

## Dependencies

- iter-001 的 `useDictionaryIndex.ts` 和 `dictionaryIndex.ts`
- `dictionaryCache.ts` (已存在)

## Test Strategy

- 单元测试: useDictionaryIndex 多词典管理
- 集成测试: DictionaryBrowser + useDictionaryIndex
- 全量回归: 1714+ 测试零回归

## Acceptance Criteria

1. ✅ useDictionaryIndex 支持多词典索引管理
2. ✅ 词典切换时索引正确重建
3. ✅ 已加载词典的索引复用（不重复构建）
4. ✅ clearAll() 清除所有词典缓存
5. ✅ 全量测试零回归
# epic-079 iter-002 Plan: 完成全部视图迁移，App.tsx 删除旧分支逻辑

## Iteration Context
- **Parent Epic:** epic-079 (App.tsx 视图路由配置中心强制落地)
- **Iteration:** iter-002 - 完成全部视图迁移
- **Previous Work:** iter-001 established ViewRouter registry pattern (v0.82.0)
- **Current Status:** iter-002 started but incomplete, App.tsx still has 9 `view === 'practice'` conditions

## Scope
frontend-only (纯前端重构)

## Work Remaining

### 分析剩余的 view === 'practice' 条件 (9 处)

1. **Lines 739-740** (LoadingScreen):
   ```tsx
   if (isLoading && view === 'practice') {
   ```
   → 应移除 view === 'practice'，改为仅 `if (isLoading)`

2. **Lines 744-745** (ErrorScreen):
   ```tsx
   if (error && view === 'practice') {
   ```
   → 应移除 view === 'practice'，改为仅 `if (error)`

3. **Lines 758-759** (EmptyState):
   ```tsx
   if (!currentSentence && !state.isComplete && view === 'practice' && !practiceSentenceIds) {
   ```
   → 应移除 view === 'practice'

4. **Lines 772-942** (Header 条件渲染):
   ```tsx
   {!(isFocusMode && view === 'practice' && !state.isComplete) && (
   ```
   → 保留（焦点模式下隐藏 header 的逻辑正确）

5. **Lines 809-874** (Header practice 控件):
   ```tsx
   {!state.isComplete && view === 'practice' && (
   ```
   → 应移除 view === 'practice'，practice 时即显示此控件

6. **Lines 870-872** (PersonalPracticePanel):
   ```tsx
   {!isPersonalMode && view === 'practice' && (
   ```
   → 应移除 view === 'practice'

7. **Lines 878-888** (Desktop nav DictionarySelector):
   ```tsx
   {view === 'practice' && (
   ```
   → 应移除 view === 'practice'

8. **Lines 945-969** (FocusModeFloatingBar):
   ```tsx
   {isFocusMode && view === 'practice' && !state.isComplete && (
   ```
   → 可保留（此逻辑在非 practice view 时无意义）

9. **Line 1353** (FocusModeOverlay practiceComponent):
   ```tsx
   view === 'practice' && !state.isComplete ? (
   ```
   → 可保留

## Implementation Steps

### Step 1: 错误边界条件简化
- 修改 LoadingScreen 条件：移除 `view === 'practice'`
- 修改 ErrorScreen 条件：移除 `view === 'practice'`
- 修改 EmptyState 条件：移除 `view === 'practice'`

### Step 2: Header 控件条件简化
- 修改 PracticeControls 条件：移除 `view === 'practice'`
- 修改 PersonalPracticePanel 条件：移除 `view === 'practice'`
- 修改 DesktopNav DictionarySelector 条件：移除 `view === 'practice'`

### Step 3: FocusModeFloatingBar 和 FocusModeOverlay
- 保留 view === 'practice' 条件（逻辑语义正确）

### Step 4: 更新测试
- 更新 App.tsx 测试中的 view 条件 mock

### Step 5: 全量测试验证
- 运行 npm test 确认零回归

## Files to Modify
- `src/App.tsx` - 简化条件逻辑

## Dependencies
- iter-001 完成的 ViewRouter 注册中心基础设施

## Test Strategy
- 2405 现有测试零回归
- 重点验证错误边界、loading、empty state 在非 practice view 时正确行为